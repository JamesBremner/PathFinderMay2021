#include "cFarmPathFinder.h"

main(int argc, char *argv[])
{
    // introduce ourself
    std::cout << "Farms\n";

    // check command line
    if (argc != 2)
    {
        std::cout << "usage: farms <inputfilename>\n";
        exit(0);
    }
}